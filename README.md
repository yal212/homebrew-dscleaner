# homebrew-dscleaner

Homebrew tap for [DScleaner](https://github.com/yal212/DScleaner) — a CLI that finds
and deletes macOS `.DS_Store` files.

## Install

```sh
brew tap yal212/dscleaner
brew install dscleaner
```

Or in one line:

```sh
brew install yal212/dscleaner/dscleaner
```
